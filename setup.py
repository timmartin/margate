from setuptools import setup

setup(name="margate",
      version='0.0.1',
      description='Faster Django templates',
      author='Tim Martin',
      author_email='tim@asymptotic.co.uk',
      license='MIT',
      packages=['margate'],
      zip_safe=False)
